// import type React from 'react';
// import UserService from '@/store/apis/auth';

// interface PropsType {
//   children: string | React.JSX.Element | React.JSX.Element[] | (() => React.JSX.Element);
// }

// const RenderOnAnonymous = ({ children }: PropsType): React.JSX.Element | null =>
//   !UserService.isLoggedIn() ? (children as React.JSX.Element) : null;

// export default RenderOnAnonymous;
